//Question 1 b) Write a class called Layer that is used to calculate a layer of the neural network

class Layer
{
	int N_node_c = N_node;
	long int s = -13;
	ran(s);

	for (int i = 0; i < 50; i++)
			{
				for (int j = 0; j < 100; j++)
						{	
							w_c[][] = ran(s) * 2 - 1;
						}
				bias_c[i] = ran(s) * 2 - 1;
			}
};